# url-proxy
url proxy

# url-proxy

Use:  
`https://<yourname>.github.io/url-proxy/?u=https://<username>.codeberg.page/<project>/`

- Restricted to Codeberg targets to avoid abuse.  
- Works on static GitHub Pages.